/*
* Copyright 2026 NXP
* NXP Proprietary. This software is owned or controlled by NXP and may only be used strictly in
* accordance with the applicable license terms. By expressly accepting such terms or by downloading, installing,
* activating and/or otherwise using the software, you are agreeing that you have read, and that you agree to
* comply with and are bound by, such license terms.  If you do not agree to be bound by the applicable license
* terms, then you may not retain, install, activate or otherwise use the software.
*/

/**
 * ------------------------------------------------------------------------------
 * NOTICE: This is an auto-generated file
 * ------------------------------------------------------------------------------
 * This file was automatically generated by GUI Guider (NXP Semiconductors).
 * Please be aware that any manual modifications may be overwritten when the
 * code is regenerated.
 *
 * Tool            : GUI Guider
 * Version         : 2.0.0
 * Generated on    : 2026/8/18
 * ------------------------------------------------------------------------------
 */

#include "gg_event.h"
#include "custom.h"
#include "custom.h"

extern bool AclockisValid;
extern int AClock_Hour_Value;
extern int AClock_Min_Value;
extern int AClock_Sec_Value;

static lv_anim_t anim;

static void screen_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_SCREEN_LOADED: {
            lv_anim_init(&anim);
            lv_anim_set_duration(&anim, 500);
            lv_anim_set_delay(&anim, 200);
            lv_anim_set_path_cb(&anim, lv_anim_path_overshoot);
            lv_anim_set_repeat_count(&anim, 0);
            lv_anim_set_repeat_delay(&anim, 0);
            lv_anim_set_var(&anim, ui->home_analog.nxp_log);

            lv_anim_set_exec_cb(&anim, anim_x_cb);
            lv_anim_set_values(&anim, 100, -30);
            lv_anim_start(&anim);

            if(!AclockisValid) {
                lv_anim_t anim_needle;
                lv_anim_init(&anim_needle);
                lv_anim_set_duration(&anim_needle, 800);
                lv_anim_set_delay(&anim_needle, 100);
                lv_anim_set_path_cb(&anim_needle, lv_anim_path_ease_in_out);
                lv_anim_set_repeat_count(&anim_needle, 0);
                lv_anim_set_exec_cb(&anim_needle, (lv_anim_exec_xcb_t)lv_image_set_rotation);

                lv_anim_set_var(&anim_needle, ui->home_analog.img_Sec);
                lv_anim_set_values(&anim_needle, 0, (AClock_Sec_Value * 6));
                lv_anim_start(&anim_needle);

                lv_anim_set_var(&anim_needle, ui->home_analog.img_Min);
                lv_anim_set_values(&anim_needle, 0, (AClock_Min_Value * 60));
                lv_anim_start(&anim_needle);

                lv_anim_set_var(&anim_needle, ui->home_analog.img_Hour);
                lv_anim_set_values(&anim_needle, 0, (AClock_Hour_Value * 300));
                lv_anim_set_completed_cb(&anim_needle, analog_clock_ready_cb);
                lv_anim_start(&anim_needle);
            }

            break;
        }
        case LV_EVENT_SCREEN_UNLOADED: {
            AclockisValid = false;
            lv_image_set_rotation(ui->home_analog.img_Sec, 0);
            lv_image_set_rotation(ui->home_analog.img_Min, 0);
            lv_image_set_rotation(ui->home_analog.img_Hour, 0);
            lv_obj_set_x(ui->home_analog.nxp_log, 100);

            break;
        }
        case LV_EVENT_GESTURE: {
            lv_indev_t * indev = lv_event_get_indev(e);
            if(indev != NULL) {
                switch(lv_indev_get_gesture_dir(indev)) {
                    case LV_DIR_LEFT: {
                        gg_screen_load_cfg_t cfg = {
                            .scr       = &ui->home_digital.screen,
                            .setup_fn  = setup_home_digital,
                            .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                            .time      = 200,
                            .delay     = 0,
                            .auto_del  = true,
                        };
                        gg_load_screen_animation(ui, &cfg);

                        break;
                    }
                    case LV_DIR_RIGHT: {
                        gg_screen_load_cfg_t cfg = {
                            .scr       = &ui->contact_list.screen,
                            .setup_fn  = setup_contact_list,
                            .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                            .time      = 200,
                            .delay     = 0,
                            .auto_del  = true,
                        };
                        gg_load_screen_animation(ui, &cfg);

                        break;
                    }
                    case LV_DIR_TOP: {
                        gg_screen_load_cfg_t cfg = {
                            .scr       = &ui->weather.screen,
                            .setup_fn  = setup_weather,
                            .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                            .time      = 200,
                            .delay     = 0,
                            .auto_del  = true,
                        };
                        gg_load_screen_animation(ui, &cfg);

                        break;
                    }
                    default:
                        break;
                }
            }
            break;
        }
        default:
            break;
    }
}

static void img_sport_text_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->fitness.screen,
                .setup_fn  = setup_fitness,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 200,
                .delay     = 0,
                .auto_del  = true,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void message_text_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->message_info.screen,
                .setup_fn  = setup_message_info,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 200,
                .delay     = 0,
                .auto_del  = true,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void sport_icon_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->fitness.screen,
                .setup_fn  = setup_fitness,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 200,
                .delay     = 0,
                .auto_del  = true,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void img_message_icon_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->message_info.screen,
                .setup_fn  = setup_message_info,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 200,
                .delay     = 0,
                .auto_del  = true,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

static void battery_text_event_handler(lv_event_t * e)
{
    gg_ui_t * ui = lv_event_get_user_data(e);

    lv_event_code_t code = lv_event_get_code(e);

    switch(code) {
        case LV_EVENT_CLICKED: {
            gg_screen_load_cfg_t cfg = {
                .scr       = &ui->battery.screen,
                .setup_fn  = setup_battery,
                .anim_type = LV_SCREEN_LOAD_ANIM_FADE_IN,
                .time      = 200,
                .delay     = 0,
                .auto_del  = true,
            };
            gg_load_screen_animation(ui, &cfg);

            break;
        }
        default:
            break;
    }
}

void gg_event_init_home_analog(gg_ui_t * ui)
{
    lv_obj_add_event_cb(ui->home_analog.screen, screen_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->home_analog.img_sport_text, img_sport_text_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->home_analog.message_text, message_text_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->home_analog.sport_icon, sport_icon_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->home_analog.img_message_icon, img_message_icon_event_handler, LV_EVENT_ALL, ui);
    lv_obj_add_event_cb(ui->home_analog.battery_text, battery_text_event_handler, LV_EVENT_ALL, ui);
}
